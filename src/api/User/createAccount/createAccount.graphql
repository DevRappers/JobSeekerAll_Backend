type Mutation {
	createAccount(
		username: String!
		email: String!
		password: String!
		age: Int!
		department: String!
		area: String
		phoneNumber: String
	): Boolean!
}

type Query {
	something: String!
}
